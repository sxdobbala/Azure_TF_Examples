apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: default-user-policyrole
  labels:
    kubernetes.io/cluster-service: "true"
    addonmanager.kubernetes.io/mode: Reconcile
rules:
- apiGroups:
  - extensions
  resourceNames:
  - default-user 
  resources:
  - podsecuritypolicies
  verbs:
  - use
