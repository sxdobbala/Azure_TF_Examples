kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-cassandra-ns
spec:
  podSelector:
    matchLabels: 
     app: cassandra
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: member-api
