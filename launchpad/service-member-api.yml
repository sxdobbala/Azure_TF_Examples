apiVersion: v1
kind: Service
metadata:
   name: member-api
   labels:
     app: member-api
spec:
   type: NodePort
   ports:
     - port: 8080
       nodePort: 32653
       name: http
       protocol: TCP
   selector:
     app: member-api
