package main

import (
	"net/http"
	"fmt"
)

var htmlLandingPage = `
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>$originalAppName</title>
  </head>
  <body>
    <h3>The $originalAppName service is alive!</h1>
  </body>
</html>
`

func landingpage(w http.ResponseWriter, req *http.Request) {
    fmt.Fprint(w, htmlLandingPage)
    return
}
